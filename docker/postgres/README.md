# PostgreSQL Docker Configuration

This directory is reserved for database initialization and containerization configurations.
In future milestones, any custom entrypoint scripts or pre-initialization SQL required before Flyway migrations run will be placed here.
