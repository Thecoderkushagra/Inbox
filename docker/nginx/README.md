# NGINX Docker Configuration

This directory is reserved for the NGINX reverse proxy configuration.
In future milestones, `nginx.conf` and SSL configuration details will be implemented here to orchestrate traffic to the frontend and backend in production.
