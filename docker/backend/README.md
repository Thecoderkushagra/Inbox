# Backend Docker Configuration

This directory is reserved for backend containerization configurations.
In future milestones, the `Dockerfile.dev` and `Dockerfile.prod` will be implemented here to build the Java Spring Boot application.
